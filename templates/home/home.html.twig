{% extends 'base.html.twig' %}
{% block body %}
<!-- template de la home page -->

  <div class="home-wrapper d-flex justify-content-center">
    <div class="test align-self-center">
      <div class="home-love">
      </div>
        <h1 class="mb-3">
          Job-Tinder
        </h1>
        <div class="text-center">
          <h5>
            Find your love job
          </h5>
          <a class="btn btn-outline-secondary" href="{{ path('user_sign') }}">Espace Candidat </a>
          <a class="btn btn-outline-secondary" href="{{ path('recruiter_sign') }}">Espace Recruteur</a>
        </div>
      
    </div>
  </div>
{% endblock %}
