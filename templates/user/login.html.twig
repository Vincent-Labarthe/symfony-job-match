{% extends 'base.html.twig' %}

{% block body %}
  <div class="login-wrapper d-flex justify-content-center ">
    <div class="sign-up col-4 align-self-center">
    <h2 class="text-center">Sign Up</h2>
     {{form(formView)}}
    </div>

    <div class="col-4 align-self-center">
      
    </div>
  </div>
{% endblock %}
